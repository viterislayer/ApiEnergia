# Copilot Instructions

## Project Guidelines
- When configuring Fluent API in EnergiaDbContext.cs, map the new tables cliente_luz and contador_energia (not old titular_servicio).
- Use 'Credencial' as the login DTO property name instead of 'Usuario' to match Core Bancario nomenclature.